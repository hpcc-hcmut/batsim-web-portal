[pytest]
testpaths = tests
markers =
    unit: Unit tests (no DB, no network)
    integration: Integration tests (test DB + API)
    e2e: End-to-end tests (requires Docker)
    e2e_docker: Real orchestrator E2E (requires Docker + BatSim images, slow)
addopts = -v --tb=short --strict-markers
